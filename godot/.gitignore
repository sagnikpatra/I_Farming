# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_presets.cfg

# Generated Gradle Android build scaffolding (regenerable via
# `godot --install-android-build-template`; added ADR-0003 Phase 1, 2026-08-22)
android/

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores (not used — GDScript per ADR-0002 — kept for safety)
.mono/
data_*/

# System/tool-specific ignores
.DS_Store
