## Smoke Check Report
**Date**: 2026-08-21
**Sprint**: Not formally tracked — this project has no `production/sprints/` or `production/epics/*/story-*.md` structure (EPICs were tracked directly in `docs/architecture/godot-migration-roadmap.md` and executed conversationally, not broken into formal story files). Scope for this check: the whole `godot/` project as it stands after EPIC-M0 through EPIC-M7.
**Engine**: Godot 4.7.1 (GDScript)
**QA Plan**: Not found — this project has never run `/qa-plan` in its literal form (see note above; the roadmap + design/gdd/ GDDs have served that role informally throughout this migration)
**Argument**: sprint (adapted — see notes above)

---

### Automated Tests

**Status**: PASS (328 tests, 328 passing)

Test framework: GUT (Godot Unit Test) — not GDUnit4 as this skill's default command assumes; this project decided GUT during EPIC-M2 (see `.claude/docs/technical-preferences.md`). Ran via:
```
godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit
```
Confirmed stable across 4 consecutive runs this session (no flakiness). One real coverage gap was found and closed during this check — see below.

---

### Test Coverage

No formal story-file coverage scan was possible (no `production/epics/*/story-*.md` exists in this project). Coverage assessed by system area instead, from `godot/tests/unit/`'s 28 test files against `godot/scripts/`'s actual source files:

| Area | Coverage Status |
|---|---|
| Economy (crop lifecycle, land/structures, farmhouse, mandi, save system, persistence bugfix, plot state, worker economy) | COVERED — every economy file has a matching test file |
| Village board core (`village_snapshot_mapper.gd`, zone/plot layout, walkable-tile occupancy) | COVERED |
| EPIC-M6 villagers (`villager.gd`, `villager_roamer.gd`, `villager_spawner.gd`, `walkable_grid.gd`) | COVERED |
| EPIC-M7 workers (`worker_assignment_row.gd`, `game_economy.gd`'s worker functions) | COVERED |
| UI sheets (farmhouse/mandi/polyhouse/agroforestry/niche-farming/events/seed-picker/decoration-picker/agro-plant-picker tabs, bottom sheet, hud) | COVERED (pure `build_view_data()`-style logic tested; node construction/styling deliberately not, per this project's own testing standard — Visual/Feel gets screenshot evidence, not automated tests) |
| `open_field_tab.gd` (new this session, EPIC-M7) | **Was MISSING, now COVERED** — found during this check (every other tab has a test file, this one didn't), added `test_open_field_tab.gd` (a construction smoke test — `OpenFieldTab` has no pure logic of its own to test, all real logic lives in the already-tested `WorkerAssignmentRow` it hosts), verified passing, confirmed stable across 3 repeat runs |
| `board_interactor.gd` (tap/drag/pan/pinch gesture state machine) | **MISSING** — no dedicated test file exists for this file at all, despite it being genuinely complex Logic/Integration-type code (a 5-state gesture machine: IDLE/PENDING_TAP/PANNING_CAMERA/DRAGGING_ZONE/DRAGGING_DECORATION). Flagged as advisory, not fixed in this pass — real unit-testing this would mean either extracting its gesture-resolution logic into pure functions (a real refactor) or building input-simulation test scaffolding, both larger efforts than this smoke-check pass should absorb unplanned. Recommend a dedicated future session. |
| `worker_station.gd`, `camera_rig.gd`, `village_fixture_data.gd`, `zone_fixture.gd`, `plot_fixture.gd` | No dedicated test file, but simple/data-shape classes or covered indirectly via board-level tests in `test_village_snapshot_mapper.gd` — judged EXPECTED/acceptable, not a gap |

**Summary**: 1 real gap found and closed this pass (`open_field_tab.gd`); 1 real gap found and left open, flagged for a future session (`board_interactor.gd`); everything else genuinely covered.

---

### Manual Smoke Checks

All three batches confirmed by the user, backed by this session's own extensive on-device verification (real touch input, ground-truth save-file reads, multiple relaunches):

- [x] Game launches without crash — PASS
- [x] Board/session loads correctly — PASS
- [x] Basic navigation (tap, drag, pan, zoom) — PASS
- [x] EPIC-M6 villagers (roaming, visuals, no crashes) — PASS
- [x] EPIC-M7 worker assignment (UI, automation, wages) — PASS
- [x] Pre-existing features (planting, harvesting, trading, zone drag) — PASS, no regression reported
- [x] Save / load — PASS
- [x] Performance — PASS (no frame-rate cost measured from villagers/workers at the population cap, on the AVD emulator — real-hardware performance testing remains a separate, still-open project-wide gap per EPIC-M0, not something this check resolves)

---

### Missing Test Evidence

- **`board_interactor.gd`** — Logic/Integration-type gesture-handling code with no automated test file. Not blocking (extensively verified via on-device testing throughout this project's history, including this session), but a real gap worth closing in a future dedicated pass.

---

### Verdict: PASS

All automated tests pass (328/328), all manual smoke checks pass, the one coverage gap found (`open_field_tab.gd`) was closed during this same check. `board_interactor.gd`'s missing test coverage is advisory, not blocking — flagged for follow-up, not a reason to withhold a PASS verdict given its extensive on-device verification history.

---

### Process notes (for future runs of this skill on this project)

- This project has no `production/sprints/` or story-file structure — `/qa-plan`'s Phase 1 scope resolution doesn't apply cleanly. This check adapted by treating "the whole godot/ project, post EPIC-M0-M7" as scope.
- This project uses GUT, not GDUnit4 — future smoke checks should use `godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`, not this skill's default GDUnit4 command.
- No CI (`.github/workflows/`) exists yet — every test run so far, across this whole project's history, has been manual/on-demand.
